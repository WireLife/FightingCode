#include<iostream>
#include<cstdlib>
#include<cstdio>
using namespace std;
char a[100];

double  f()
{
	scanf("%s",&a);
	if(strlen(a)==1&&!(a[0]>='0'&&a[0]<='9'))
	{
		switch(a[0])
		{
			case '+': return f()+f();break;
			case '-': return f()-f();break;
			case '*': return f()*f();break;
			case '/': return f()/f();break;
		}
	}
	else return atof(a);
}
int main()
{
	double a;
	a =f();
	cout<<a;
	system("pause");
	return 0;
}
