#include<iostream>
#include<string>
using namespace std;
void main()
{
	int a[256][2]={0},k,len,i;
	string  str;
	getline(cin,str);
	len= str.size();
	k=0;
	for(i=0;i<len;i++)
	{
		if (a[(int)str[i]][1]==0) a[(int)str[i]][0]=i;  //记录第一次出现的位置
		a[(int)str[i]][1]++; //记录出现的次数
	}
	k=256;
	for(i=0;i<256;i++){  //找只出现一次且最早出现
		if((a[i][1]==1)&&(k>a[i][0])) k=a[i][0];
	}
	if(k>255)
		printf("no");
	else printf("%c",str[k]);
}
