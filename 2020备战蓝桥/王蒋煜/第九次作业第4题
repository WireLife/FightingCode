#include<stdio.h>
int a[1000001];
int f(int n)
{
	if(a[n]!=0) return a[n];
	if(n==1) return 1;
	if(n==2) return 2;
	return a[n]=2*(f(n-1))+f(n-2);
}
int main()
{
    int k,n,i,j;
    scanf("%d",&n);
    for(j=1;j<=n;j++)
    {
	  scanf("%d",&k);
	  printf ("%d",f(k));
    }
    return 0;
}
