#include<stdio.h>
int Pell(int n)
{
    if(n==1)
        return 1;
    else if(n==2)
        return 2;
    else
        return Pell(n-1)+Pell(n-2);
}
int main()
{
    int k;
    scanf("%d",&k);
    printf("%d",Pell(k));
    return 0;
}
