#include<iostream>
using namespace std;
int f(int n,int m);
int main()
{
	int limit;
	cin>>limit;
	while(limit--)
	{
		int n,m;
		cin>>m>>n;
		cout<<f(n,m)<<endl;
	}
	return 0;
}
int f(int n,int m)
{
	if(m==0||n==1)
		return 1;
	if(n>m)
		return f(m,m);
	else
		return f(n,m-n)+f(n-1,m);
}
